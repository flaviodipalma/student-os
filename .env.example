# Copy this file to .env.local and fill in the values. Never commit .env.local.

# ---- Supabase: database + login ----------------------------------------------
# From your Supabase project: Project Settings > API (or the "Connect" button).
# These two are public by design (the NEXT_PUBLIC_ prefix sends them to the browser);
# every table has Row Level Security on, so they can't read Student OS data.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Postgres connection string, SERVER-ONLY (no NEXT_PUBLIC_ prefix). Use the
# connection pooler URI ("Transaction" mode, port 6543) from Connect > ORMs/Drizzle,
# with your database password filled in.
DATABASE_URL=

# ---- AI: syllabus import -----------------------------------------------------
# Claude API key, server-only. Get one at https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Optional: which Claude model reads syllabi (default: claude-opus-5).
# Options: claude-opus-5, claude-sonnet-5, claude-haiku-4-5
# SYLLABUS_AI_MODEL=claude-opus-5

# Optional: "mock" swaps the AI for a simple local pattern matcher, for
# development without an API key. It is NOT real AI; don't use it for real syllabi.
# SYLLABUS_AI_PROVIDER=anthropic

# ---- LMS integrations ---------------------------------------------------------
# All SERVER-ONLY: never add a NEXT_PUBLIC_ prefix. Setup guide:
# src/server/integrations/lms/README.md ("Canvas setup").
#
# Key that encrypts stored LMS access/refresh tokens (AES-256-GCM): 32 random
# bytes, base64. Generate with:  openssl rand -base64 32
# Keep it in your host's secret settings; changing it means students reconnect.
# LMS_TOKEN_ENCRYPTION_KEY=
#
# Canvas: from an API developer key created by your school's Canvas admin
# (Admin > Developer Keys > + Developer Key > API Key). The Client ID is the
# key's ID; the Client Secret is its "Key" value (click "Show Key").
# CANVAS_CLIENT_ID=
# CANVAS_CLIENT_SECRET=
#
# Must match a Redirect URI on the developer key exactly. Locally:
# CANVAS_REDIRECT_URI=http://localhost:3000/api/integrations/canvas/callback
#
# Optional: Canvas addresses students may connect to (comma-separated; "*." allows
# subdomains). Default: *.instructure.com. Add your school's own Canvas domain if
# it's self-hosted, e.g. canvas.myschool.edu
# CANVAS_ALLOWED_HOSTS=*.instructure.com
#
# Optional: set to "false" if the developer key does NOT use "Enforce Scopes"
# (by default Student OS asks for read-only scopes: courses and assignments).
# CANVAS_REQUEST_SCOPES=true
#
# Blackboard: not implemented yet. Reserved names:
# BLACKBOARD_CLIENT_ID=
# BLACKBOARD_CLIENT_SECRET=

# ---- Development seed (optional) ----------------------------------------------
# `npm run db:seed` fills this ONE account with sample courses, tasks and events.
# Use an address you control; real accounts are never touched.
# DEV_SEED_EMAIL=
# DEV_SEED_PASSWORD=
